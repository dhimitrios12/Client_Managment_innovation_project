import 'package:flutter/material.dart';

class RandomTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is a random screen'),
      ),
    );
  }
}
